PORT=5000
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/dhi_test_tracking
JWT_SECRET=change-me-in-production

# Mailgun (email notifications — utilisé sur Render, pas de blocage SMTP)
MAILGUN_API_KEY=
MAILGUN_DOMAIN=

# SMTP (fallback local)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
EMAIL_FROM="DHI Test Tracking <noreply@dhi-test-tracking.com>"
